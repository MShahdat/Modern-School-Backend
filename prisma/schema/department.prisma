model Department {
  id String @id @default(uuid())

  name        String
  icon        String?
  description String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  teacher Teacher[]

  @@map("Departments")
}
